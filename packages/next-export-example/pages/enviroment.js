import Nav from '../components/Nav';

export default function Environment() {
  return (
    <main style={{ padding: '24px', fontFamily: 'system-ui' }}>
      <Nav />
      <h1>Environment & Stress</h1>
      <p>
        Weather, footing, hauling, and routine changes affect performance and health.
        This section outlines key adjustments for each.
      </p>
    </main>
  );
}
